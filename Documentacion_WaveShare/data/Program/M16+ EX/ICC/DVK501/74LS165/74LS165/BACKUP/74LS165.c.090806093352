/*********************************************************************
	微 雪 电 子   WaveShare   http://www.waveShare.net            	
		                                                            
项目名称:   74LS164示例程序
		
目标系统:   “DVK501” && “M48+ EX”

应用软件:   ICCAVR 6.31A                                             
		                                                                
版    本:   V1.0 
                                                        
圆版时间:   2009-7-1

开发人员:   zz

说    明:   若用于商业用途，请保留此段文字或注明代码来源
		
	深 圳 市 微 雪 电 子 有 限 公 司 保 留 所 有 的 版 权     
*********************************************************************/

/*01010101010101010101010101010101010101010101010101010101010101010101
----------------------------------------------------------------------
版本更新记录：

----------------------------------------------------------------------
实验内容：
使用开发板的LED指示灯查看MCU自己从模拟串口发送的数据。
----------------------------------------------------------------------
硬件连接： 
          	  DVK501     			DVK501
				Q0		---------     +/-
				Q1  	---------     +/-
				Q2 		---------     +/-
				Q3  	---------     +/-
				Q4  	---------     +/-
				Q5  	---------     +/-
				Q6  	---------     +/-
				Q7  	---------     +/-
		 	  DVK501				 Mega48
				PL		---------	  PD3
				QH		---------	  PC0
				CLK		---------	  PC1
----------------------------------------------------------------------
注意事项：
（1）若有加载库函数，请将光盘根目录下的“库函数”下的“ICC_H”文件夹拷到D盘
（2）请详细阅读“使用必读”及相关资料。
----------------------------------------------------------------------
10101010101010101010101010101010101010101010101010101010101010101010*/
#include <iom48v.h>
#include "D:\ICC_H\CmmICC.H"

#define DISP_DDR    DDRD
#define DISP_PORT   PORTD

#define IN_DATA		cbi(DDRD,0)	   //PD0 数据口
#define GET_DATA	gbi(PIND,0)

#define OUT_CLK  	sbi(DDRD,1)	   //PD1 脉冲口
#define SET_CLK 	sbi(PORTD,1)
#define CLR_CLK  	cbi(PORTD,1)

#define OUT_PL		sbi(DDRC,4)	   //PC4 锁存口
#define SET_PL		sbi(PORTC,4)
#define CLR_PL		cbi(PORTC,4)

/*--------------------------------------------------------------------
函数名称：
函数功能：
注意事项：
提示说明：
输    入：
返    回：
--------------------------------------------------------------------*/
uint8 getDat()
{
	uint8 i,dat;
	for(i=0;i<8;i++)
	{
		CLR_CLK;
		dat = dat<<1;
		if(GET_DATA)
			dat |= 0x01;
		SET_CLK;
	}
	return dat;
}
/*--------------------------------------------------------------------
函数名称：
函数功能：
注意事项：
提示说明：
输    入：
返    回：
--------------------------------------------------------------------*/
void main(void)
{
	uint8 i;	
	while(1)
	{
   	OUT_PL;
	 OUT_CLK;
	 IN_DATA;
		SET_PL;
		CLR_PL;
		SET_PL;
		DISP_PORT = getDat();
    DISP_DDR = 0XFF;
    delay50ms(4);
	}
}
